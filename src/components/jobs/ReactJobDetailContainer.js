import React, { Component } from 'react';
import { Segment, Header } from 'semantic-ui-react';

export default class ReactJobDetailContainer extends Component {
    render() {
        return (
            <Segment basic color="purple" inverted>
                <Header>Job detail container</Header>
            </Segment>
        )
    }
}
